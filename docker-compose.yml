services:
  confabchef:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - LAT=${LAT}
      - LNG=${LNG}
      - MARKETS=Lidl,EDEKA
